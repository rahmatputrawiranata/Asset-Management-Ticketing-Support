<div class="form-group">
    <div class="col-form-label">{{$title}}</div>
    <select class="form-controller select2" style="width: 100%" name="{{$name}}{{$multiple ? '[]' : ''}}" id="{{$name}}" {{$multiple ? 'multiple' : ''}}>

    </select>
</div>
